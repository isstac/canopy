


@using canopy


shell=edu.cmu.sv.isstac.canopy.mcts.MCTSShell
#symbolic.dp=choco

symbolic.dp=z3
target.args=8

target=sampling.wise.SortedListInsert


symbolic.method=sampling.wise.SortList.insert(con),sampling.SortList.insertMask(con)
symbolic.heuristic.measuredmethods=sampling.wise.SortList.insert(con)

canopy.stats=true
canopy.livestats=true

canopy.modelcounting.amplifyrewards=false

canopy.mcts.weightedsampling=false

canopy.mcts.treevisualizer=false
